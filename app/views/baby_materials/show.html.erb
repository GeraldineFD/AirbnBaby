<div class="container">
  <div class="box">
  <div class="card-product">
    <%= cl_image_tag @baby_material.photo, height: 300, width: 400, crop: :fill %>
    <div class="card-product-infos">
      <h2><%= @baby_material.title %></h2>
      <p><strong>Description:</strong> <%= @baby_material.description %></p>
      <p><strong>Adresse:</strong> <%= @baby_material.address %></p>
      <p><strong>Prix:</strong> <%= @baby_material.price %> €</p>
    </div>
  </div>
  <div class="button margined">
 <%= link_to "Delete", baby_material_path, method: :delete, data: { confirm: 'Are you sure'} , class: "btn btn-flat" %>
  <%= link_to "Edit", edit_baby_material_path, class: "btn btn-flat" %>
  <%= link_to "Buy", admin_baby_material_index_path, class: "btn btn-flat" %>
  </div>
  </div>
</div>
